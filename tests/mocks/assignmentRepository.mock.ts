import { IAssignmentRepository } from '@src/application/interfaces/IAssignmentRepository';
import { mock } from 'jest-mock-extended';

export const assignmentRepositoryMock = mock<IAssignmentRepository>();