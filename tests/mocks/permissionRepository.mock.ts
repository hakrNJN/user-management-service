import { IPermissionRepository } from '@src/application/interfaces/IPermissionRepository';
import { mock } from 'jest-mock-extended';

export const permissionRepositoryMock = mock<IPermissionRepository>();