import { IPolicyEngineAdapter } from '@src/application/interfaces/IPolicyEngineAdapter';
import { mock } from 'jest-mock-extended';

export const policyEngineAdapterMock = mock<IPolicyEngineAdapter>();